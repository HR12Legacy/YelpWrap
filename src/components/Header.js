import React from 'react';
import style from './header.css';
//Grab all top components dependencies

const Header = (props) =>(
  <div className={style.header}>
    {/* Grab all top components */}
 </div>

)

export default Header;